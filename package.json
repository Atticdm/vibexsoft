{
  "name": "vibexsoft-site",
  "version": "1.0.0",
  "private": true,
  "description": "VibeX LLC corporate website",
  "license": "UNLICENSED",
  "engines": { "node": ">=20" },
  "scripts": {
    "start": "node server.js",
    "dev": "ENFORCE_CANONICAL=false node server.js",
    "check": "node --check server.js && node scripts/check.js"
  }
}
